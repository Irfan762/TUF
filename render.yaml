services:
  - type: web
    name: tuf-calendar
    env: static
    rootDir: calendar-app
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
